# Skills

Agent skills for the Straddle API.

## Repository layout

Published skills live at `skills/<skill-name>/SKILL.md`. The directory name must be kebab-case and match the `name` in the skill's YAML frontmatter. Every skill must also define a non-empty `description`.
